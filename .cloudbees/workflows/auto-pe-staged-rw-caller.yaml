apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My workflow
on:
  workflow_dispatch:
metadata:
  stages/v1alpha1:
    - name: Reusable Stage
      jobs:
        - call-reusable
jobs:
  call-reusable:
    uses: .cloudbees/workflows/auto-pe-reusable-wf.yaml
